# Типы данных

Rust — **статически типизированный язык**. Компилятор должен знать тип каждой переменной.
Обычно он сам догадывается (выводит типы), но иногда ему нужно помочь.

```rust
let guess: u32 = "42".parse().expect("Не число!");
```

### Скалярные типы (Одиночки)

1. **Целые числа**:
   - `i32` (знаковое, дефолт), `u32` (беззнаковое).
   - `i8`, `i16`, `i64`, `i128` (и соответствующие `u...`).
   - `isize`, `usize` (зависят от архитектуры: 32 или 64 бита). Используются для индексов массивов.

   > **Осторожно, переполнение!**

> Если в `u8` (0..255) положить 256, в дебаге будет `panic!`, а в релизе — перенос (wrap around) в 0.





2. **Числа с плавающей точкой**: `f32`, `f64` (дефолт). Стандарт IEEE-754.

3. **Логический тип (Boolean)**: `bool`. Значения: `true`, `false`. Занимает 1 байт.

4. **Символы (Char)**: `char`. Одинарные кавычки: `'z'`, `'😻'`. Это Unicode Scalar Value (4 байта). Эмодзи — это законные символы.

### Составные типы (Группировки)

1. **Кортежи (Tuples)**: Фиксированная длина, разные типы.
   ```rust
   let tup: (i32, f64, u8) = (500, 6.4, 1);
   let (x, y, z) = tup; // Деструктуризация
   let five_hundred = tup.0; // Доступ по индексу через точку
   ```

2. **Массивы (Arrays)**: Фиксированная длина, **один тип**. Живут на стеке.
   ```rust
   let a = [1, 2, 3, 4, 5];
   let months = ["Jan", "Feb", ...];
   let first = a[0];
   ```

   Если попытаетесь обратиться к индексу за пределами массива (например, `a[10]`), Rust запаникует и остановит программу. Safety first!

---

**Анекдот:**

> — Папа, а почему солнце встает на востоке?  
> — Сынок, это `const`, не трогай, а то перестанет работать.

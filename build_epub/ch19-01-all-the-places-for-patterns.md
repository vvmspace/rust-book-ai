## Паттерны повсюду! 👀

Вы думали, паттерны живут только в `match`? Ха! Они повсюду. Они как пыль, только полезные.

### `match` ветви

Тут все понятно. Классика.

```rust
match x {
    None => None,
    Some(i) => Some(i + 1),
}
```

### `if let`: Младший брат `match`

Когда вам нужно проверить только один вариант. Можно даже с `else if let`.

```rust
if let Ok(age) = age {
    if age > 30 {
        println!("Using purple as the background color");
    }
}
```

Недостаток: компилятор не проверит, что вы обработали все варианты. Вы сами за себя.

### `while let`: Пока не надоест 🔄

Пока паттерн совпадает, цикл крутится. Идеально для очередей и каналов.

```rust
while let Ok(msg) = rx.recv() {
    println!("{}", msg);
}
```

### Циклы `for`

Сюрприз! То, что после `for`, — это тоже паттерн.

```rust
for (index, value) in v.iter().enumerate() {
    println!("{} is at index {}", value, index);
}
```

Мы деструктурируем кортеж прямо на лету. Красота.

### Инструкции `let`

Да-да, `let x = 5` — это паттерн-матчинг.
`x` — это паттерн, который говорит "привяжи все, что справа, к переменной x".

Поэтому можно делать так:

```rust
let (x, y, z) = (1, 2, 3);
```

Но нельзя так (потому что типы разные):

```rust
let (x, y) = (1, 2, 3); // Ошибка!
```

### Параметры функций

Аргументы функции — это тоже паттерны.

```rust
fn print_coordinates(&(x, y): &(i32, i32)) {
    println!("Current location: ({}, {})", x, y);
}
```

---

**Анекдот:**

> Психиатр показывает пациенту картинку:  
> — Что вы видите?  
> — Я вижу `let (x, y) = point;`.  
> — Доктор, у меня апофения?  
> — Нет, у вас просто Rust головного мозга.

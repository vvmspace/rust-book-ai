## Что такое ООП (и при чем тут Rust)? 🤔

ООП — это как "крафтовое пиво". Все говорят об этом, но каждый подразумевает разное.
Обычно под ООП имеют в виду три вещи:

1. **Объекты**: Данные + Методы.
2. **Инкапсуляция**: Скрытие кишков (private implementation).
3. **Наследование**: Ребенок забирает долги родителя (шутка).

### Объекты? Есть.
В Rust есть `struct` (данные) и `impl` (методы). Технически это объекты. Даже `enum` может иметь методы. Так что галочка.

### Инкапсуляция? Есть.
Ключевое слово `pub`. Если не поставил `pub` — никто снаружи не увидит. Можно делать публичные геттеры для приватных полей. Галочка.

### Наследование? Нет.
В Rust нет наследования. И это фича, а не баг.
Наследование часто приводит к "проблеме банана": вы хотели банан, а получили гориллу, которая держит банан, и все джунгли в придачу.

Вместо наследования Rust использует **Трейты** (Traits) для шаринга поведения и **Дженерики** для полиморфизма.
Это называется "Composition over Inheritance".

### Итог
Rust — это ООП язык?
Ну, он позволяет делать всё то же самое, но без боли от глубоких иерархий наследования.

---

**Анекдот:**

> — Папа, а почему мы не можем унаследовать твое состояние?  
> — Потому что мы в Rust, сынок. Тут только композиция. И вообще, я еще из `scope` не вышел.

## Синтаксис паттернов: Полный гид 📖

В этом разделе мы соберем все возможные способы использования паттернов. Это как шведский стол, только вместо еды — синтаксик.

### Литералы

Самое простое: матчим конкретные значения.

```rust
let x = 1;
match x {
    1 => println!("one"),
    _ => println!("anything"),
}
```

### Именованные переменные и проблема "Тени" 👻

Паттерны могут создавать новые переменные. И это может быть сюрпризом (неприятным).

```rust
let x = Some(5);
let y = 10;

match x {
    Some(50) => println!("Got 50"),
    Some(y) => println!("Matched, y = {:?}", y), // Это НОВЫЙ y, а не тот, который 10!
    _ => println!("Default case, x = {:?}", x),
}

println!("at the end: x = {:?}, y = {:?}", x, y);
```

Внутри `match` создается *новая* область видимости. `Some(y)` перекрывает (shadow) внешнюю переменную `y`. Будьте бдительны.

### Множественные паттерны (`|`)

Логическое ИЛИ.

```rust
match x {
    1 | 2 => println!("one or two"),
    3 => println!("three"),
    _ => println!("anything"),
}
```

### Диапазоны (`..=`)

Хотите сматчить всё от 1 до 5?

```rust
match x {
    1..=5 => println!("one through five"),
    _ => println!("something else"),
}
```

Работает и с `char`: `'a'..='j'`.

### Деструктуризация: Ломать — не строить 🔨

Паттерны позволяют разбирать структуры, enum'ы и кортежи на части.

#### Структуры

```rust
struct Point { x: i32, y: i32 }
let p = Point { x: 0, y: 7 };
let Point { x, y } = p; // Сокращенный синтаксис
```

Можно матчить и проверять значения одновременно:

```rust
match p {
    Point { x, y: 0 } => println!("On the x axis at {}", x), // y должен быть 0
    Point { x: 0, y } => println!("On the y axis at {}", y), // x должен быть 0
    Point { x, y } => println!("On neither axis: ({}, {})", x, y),
}
```

#### Enum'ы и вложенность

Можно залезать так глубоко, как нужно.

```rust
match msg {
    Message::ChangeColor(Color::Rgb(r, g, b)) => { ... }
    Message::ChangeColor(Color::Hsv(h, s, v)) => { ... }
    _ => (),
}
```

### Игнорирование значений 🙈

Иногда вам все равно, что там внутри.

- `_`: Полный игнор. `fn foo(_: i32, y: i32)`.
- `_x`: "Я знаю, что эта переменная не используется, не ной, компилятор". Но она *все равно захватывает значение* (move semantic).
- `..`: Игнорировать остальное. `Point { x, .. }`. Или `(first, .., last)`.

Внимание: `..` не должно быть двусмысленным. `(.., second, ..)` — ошибка, потому что компилятор не знает, где середина.

### Match Guards: "Да, но если..." 💂‍♂️

Вы можете добавить условие `if` к паттерну.

```rust
let num = Some(4);

match num {
    Some(x) if x % 2 == 0 => println!("The number {} is even", x),
    Some(x) => println!("The number {} is odd", x),
    None => (),
}
```

Это решает проблему с затенением переменных!

```rust
match x {
    Some(n) if n == y => println!("Matched, n = {}", n), // Сравниваем с внешним y
    _ => println!("Default case"),
}
```

### Привязки `@`: Поймать и проверить 🕸️

Оператор `@` позволяет вам одновременно проверить значение на соответствие паттерну *и* сохранить его в переменную.

```rust
match msg {
    Message::Hello { id: id_variable @ 3..=7 } => {
        println!("Found an id in range: {}", id_variable)
    },
    Message::Hello { id: 10..=12 } => {
        println!("Found an id in another range")
    },
    Message::Hello { id } => println!("Found some other id: {}", id),
}
```

В первом случае мы проверяем, что `id` в диапазоне `3..=7` И сохраняем его в `id_variable`.
Во втором — проверяем, но *не сохраняем*.

---

**Анекдот:**

> У программиста есть проблема. Он думает: "Я решу её с помощью регулярных выражений".
> Теперь у него две проблемы.
> Слава Богу, паттерн-матчинг в Rust — это не регулярки.

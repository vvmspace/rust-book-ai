# Бесстрашная конкурентность 🦁

Раньше работа с потоками напоминала прогулку по минному полю. Один неверный шаг — и у вас `Segmentation fault`, гонка данных (data race) или дедлок.

Rust меняет правила игры.

### "Бесстрашная" (Fearless)?
Да. Это значит, что компилятор **не даст вам выстрелить себе в ногу**.
Если вы попытаетесь передать ссылку на стековую переменную в поток, который может жить дольше этой переменной — **Ошибка компиляции**.
Если вы попытаетесь изменить данные из двух потоков без синхронизации — **Ошибка компиляции**.

Rust переносит головную боль из рантайма (когда программа падает у клиента в 3 часа ночи) в компайл-тайм (когда вы пьете кофе и смотрите на сообщения об ошибках).

В этой главе мы узнаем:

1. Как создавать потоки (`spawn`).
2. Как общаться между ними (`channels`).
3. Как делиться данными (`Mutex`, `Arc`).
4. Как работают `Send` и `Sync`.

Поехали!

---

**Анекдот:**

> — В чем разница между JavaScript-разработчиком и Rust-разработчиком?  
> — JavaScript-разработчик боится конкурентности, потому что её нет.  
> — А Rust-разработчик не боится конкурентности, потому что компилятор на страже.

## Продвинутые трейты 🔥

Думали, вы все знаете про трейты? Как бы не так.
Трейты в Rust — это не просто интерфейсы. Это швейцарский нож с лазерным прицелом. В этой главе мы рассмотрим всё самое интересное.

### Ассоциированные типы 🔗

Ассоциированные типы — это плейсхолдеры внутри трейта.
Взгляните на `Iterator`:

```rust
pub trait Iterator {
    type Item; // Вот он, наш герой
    fn next(&mut self) -> Option<Self::Item>;
}
```

Почему не дженерики? Почему не `trait Iterator<T>`?
Потому что у итератора должен быть только *один* тип элемента. Если бы мы использовали дженерики, мы могли бы реализовать `Iterator<String>` И `Iterator<i32>` для одного типа. И тогда при вызове `next()` компилятор сошел бы с ума: "Какую реализацию ты хочешь?".
Ассоциированные типы говорят: "У этого типа есть ровно один тип итератора".

### Дефолтные параметры и перегрузка операторов ➕

Rust не дает создавать свои операторы (и слава Богу). Но можно перегрузить существующие, например `+`.

```rust
trait Add<Rhs=Self> {
    type Output;
    fn add(self, rhs: Rhs) -> Self::Output;
}
```

Видите `Rhs=Self`? Это дефолтный параметр. Если вы не укажете тип правого операнда, он будет таким же, как и левый (`Point + Point`).
Но можно сделать и `Other + Point` -> `BigPoint`, если очень захотеть.

### Синтаксис полной квалификации: Когда имена совпадают 👯‍♂️

Представьте, что вы — Человек (`Human`). Вы умеете летать (`fly`).
Но вы также Пилот (`Pilot`) и Волшебник (`Wizard`). И у всех есть метод `fly`.

```rust
human.fly(); // Чей это метод? Самого Human.
Pilot::fly(&human); // Как пилот.
Wizard::fly(&human); // Как волшебник.
```

А если метода нет у `self`? Если это ассоциированная функция (как конструктор)?
Тогда в бой идет тяжелая артиллерия:
`<Dog as Animal>::baby_name()`

Это называется "Fully Qualified Syntax". Используйте его, когда компилятор в замешательстве.

### Супертрейты: Наследование (но не совсем) 👨‍👦

Иногда вам нужно, чтобы ваш трейт требовал реализации другого трейта.
Например, вы хотите выводить объект в красивой рамочке (`OutlinePrint`). Для этого объект должен уметь превращаться в строку (`Display`).

```rust
trait OutlinePrint: Display { ... }
```

Это значит: "Кто хочет реализовать OutlinePrint, обязан реализовать и Display".

### Newtype Pattern и правило сироты 👶

Правило сироты (Orphan Rule): нельзя реализовать внешний трейт для внешнего типа.
Нельзя сделать `impl Display for Vec<T>`. Потому что и `Display` и `Vec` — чужие.

Но мы хитрее. Мы создадим свой тип-обертку!

```rust
struct Wrapper(Vec<String>);

impl fmt::Display for Wrapper { ... }
```

Вуаля! Теперь `Wrapper` — наш тип, и мы можем делать с ним что хотим.

---

**Анекдот:**

> — Это птица?  
> — Это самолет?  
> — Нет, это `<Superman as Hero>::fly()`.

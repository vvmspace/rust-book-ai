## Передача сообщений: Каналы 📨

"Делитесь памятью, общаясь, а не общайтесь, делясь памятью". Этот слоган Go отлично подходит и для Rust (хотя у нас есть и `Mutex` для любителей экстрима).

### Каналы `mpsc`

`mpsc` = **M**ultiple **P**roducer, **S**ingle **C**onsumer.
Это значит: "Много писателей, один читатель". Как река, в которую впадают ручьи.

### Как это работает?

```rust
use std::sync::mpsc;
use std::thread;

let (tx, rx) = mpsc::channel();

thread::spawn(move || {
    let val = String::from("Привет");
    tx.send(val).unwrap(); // Отправили
    // println!("val is {}", val); // ОШИБКА! Вал уплыл.
});

let received = rx.recv().unwrap(); // Получили (блокирует поток, пока не придет)
println!("Получено: {}", received);
```

### Важные моменты:

1. **Владение передается**: Когда вы делаете `send(val)`, вы отдаете `val` в канал. Больше вы его трогать не можете. Это гениально, потому что исключает гонки данных.
2. **Клонирование отправителя**: Хотите писать из двух потоков? `let tx1 = tx.clone();`.
3. **Итератор**: `rx` работает как итератор. `for msg in rx { ... }` будет читать, пока канал не закроется.

---

**Анекдот:**

> Один поток пишет другому:  
> — Привет, ты слышал про `mpsc`?  
> — (через 5 секунд) Да, слышал. А ты слышал про `try_recv`?  
> — Нет, я был заблокирован на `recv`.

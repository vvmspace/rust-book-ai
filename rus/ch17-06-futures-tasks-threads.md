## Фьючеры, Задачи и Потоки: Вся королевская рать 👑

Мы видели потоки. Мы видели фьючеры. Пришло время собрать их вместе.

### Потоки vs Задачи

- **Поток (Thread)**: Единица конкурентности, управляемая ОС. Тяжелая (много памяти), но прерываемая (preemptive).
- **Задача (Task)**: Единица конкурентности, управляемая рантаймом Rust. Легкая, но кооперативная (надо дождаться `.await`).

Рантайм (например, Tokio) использует пул потоков для выполнения задач. Это хитрая схема, где M задач бегают по N потокам (M:N threading), используя Work Stealing.

### Когда что использовать?

- **Тяжелые вычисления (CPU-bound)**: Используйте **ПОТОКИ**. Фьючер не поможет вам быстрее посчитать факториал. Он только заблокирует поток исполнителя.
- **Ожидание (IO-bound)**: Используйте **ASYNC**. Сеть, диск, таймеры. Зачем держать целый поток, пока мы ждем ответа от сервера?

### Mix & Match

Никто не запрещает использовать и то, и другое. Вы можете запустить тяжелый расчет в `thread::spawn`, а результат передать в канал, который слушает `async` задача.

```rust
// В async мире
let (tx, rx) = mpsc::channel();
thread::spawn(move || {
    // В синхронном мире
    let result = heavy_computation();
    tx.send(result).unwrap();
});

// Обратно в async
let result = rx.recv().await;
```

Это и есть дзен Rust: вы выбираете инструмент под задачу, а не подгоняете задачу под инструмент.

---

**Анекдот:**

> — Официант, я жду свой суп уже час!  
> — Извините, сэр, повар заблокировал поток executor'а, пытаясь вычислить число Пи.  
> — А почему он не вынес это в `spawn_blocking`?  
> — Он думал, что `async` делает всё быстрее магическим образом.

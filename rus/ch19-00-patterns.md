# Паттерны и сопоставление 🧩

Паттерны — это секретный ингредиент Rust. Это как регулярные выражения, но для типов данных. И они работают.
Мы уже видели их в `match`, но они способны на большее.

В этой главе мы разберем:

- **Где живут паттерны**: `match`, `if let`, `while let`, `for`, `let` и даже параметры функций.
- **Опровержимость (Refutability)**: Почему `let Some(x) = ...` не работает, а `if let Some(x) = ...` работает.
- **Синтаксис**: Все возможные способы написать паттерн. Литералы, диапазоны, деструктуризация, wildcards и прочее.

Готовы стать мастером деструктуризации? Поехали.

---

**Анекдот:**

> — Девушка, а вашей маме зять не нужен?  
> — Молодой человек, ваш паттерн `Zyat` является Refutable. Моя мама ожидает `Zyat { salary: u64, car: bool }`, где `salary > 100_000` и `car == true`.  
> — Panic!

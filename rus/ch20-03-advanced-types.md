## Продвинутые типы: когда `i32` уже недостаточно 🔬

Система типов Rust — это не просто способ не перепутать числа со строками. Это целая вселенная. Давайте заглянем в ее темные углы.

### Newtype Pattern: Безопасность превыше всего 🛡️

Паттерн "Newtype" — это когда мы берем существующий тип и оборачиваем его в новую структуру.

```rust
struct Millimeters(u32);
struct Meters(u32);
```

Теперь вы не сможете случайно сложить миллиметры и метры. Компилятор ударит вас линейкой по рукам.
А еще это позволяет скрыть детали реализации (инкапсуляция) или реализовать внешние трейты для внешних типов (обход правила сироты).

### Псевдонимы типов: просто другое имя 🏷️

`type` работает как `alias` в командной строке.

```rust
type Kilometers = i32;
```

`Kilometers` и `i32` — это *один и тот же* тип. Вы можете их складывать. Это просто для удобства и читаемости.
Особенно полезно, когда у вас типы длиной с железнодорожный состав:
`Box<dyn Fn() + Send + 'static>` -> `Thunk`.

### Тип Never (`!`) 🕳️

Есть функция, которая никогда не возвращается?
Например, бесконечный цикл `loop` без `break`, или `panic!`, или `std::process::exit`.
У таких функций возвращаемый тип — `!`. "Never".

```rust
fn bar() -> ! {
    panic!("This call needs to go");
}
```

Тип `!` можно привести к *любому* другому типу. Именно поэтому `match` с `continue` работает.

### Динамически размерные типы (DST) и Sized 📏

Rust любит знать, сколько места занимает тип. Но есть типы-бунтари.
`str` (не `&str`!), `[T]` (не `&[T]`!), `dyn Trait`.
Их размер неизвестен в момент компиляции. Поэтому мы **всегда** кладем их за указатель (`&`, `Box`, `Rc`).

Трейт `Sized` автоматически добавляется ко всем дженерикам: `fn foo<T>(t: T)`. Это значит `T: Sized`.
Если вы хотите разрешить DST, пишите `fn foo<T: ?Sized>(t: &T)`.
`?Sized` означает "может быть Sized, а может и нет".

---

**Анекдот:**

> Заходят как-то в бар `i32`, `String` и `!`.
> Бармен наливает `i32` и `String`.
> А `!` говорит: "А мне не надо, я все равно отсюда не выйду".

# Привет, Cargo! 📦

Cargo — это:

- Система сборки.
- Менеджер пакетов.
- Ваш лучший друг.

Серьезно, после Cargo вы будете смотреть на `Makefiles` или `CMake` как на орудия пыток из средневековья.

### Создаем проект с Cargo

Забудьте про ручное создание папок.

```console
$ cargo new hello_cargo
$ cd hello_cargo
```

Что внутри?

- `Cargo.toml`: Паспорт проекта. Тут имя, версия и зависимости. Формат TOML (Tom's Obvious, Minimal Language).
- `src/main.rs`: Ваш код. Cargo любит порядок: код — в `src`, мусор — в корзину.

### Сборка и запуск

1. **Собрать**: `cargo build`.
   - Создаст бинарник в `target/debug/hello_cargo`.        
   - Первый раз может быть долго (компилирует стандартную библиотеку), потом — мгновенно.
2. **Запустить**: `cargo run`.
   - Это комбо: и соберет (если надо), и запустит.
   - Вывод: `Hello, world!`.
3. **Проверить**: `cargo check`.
   - Просто проверяет, что код компилируется.
   - Работает в разы быстрее, чем build. Полезно, когда вы пишете код и хотите быстро узнать, где накосячили.

### Релиз!

Когда ваша программа готова покорять мир:

```console
$ cargo build --release
```

Файл будет в `target/release`. Он будет оптимизирован по самое не балуйся, поэтому компиляция займет больше времени.

### Итог

В реальной жизни вы почти никогда не будете вызывать `rustc` напрямую. Всегда используйте `cargo`.

---

**Анекдот:**

> — Тук-тук.  
> — Кто там?  
> — Cargo.  
> — Какой Cargo?  
> — Cargo check. Ошибок нет, можно открывать!

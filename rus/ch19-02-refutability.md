## Refutability: Может ли паттерн не сработать? 🤔

В Rust паттерны делятся на два лагеря:

1. **Irrefutable (Неопровержимые)**: Те, которые сработают всегда. Как гравитация.
2. **Refutable (Опровержимые)**: Те, которые могут не сработать. Как планы на выходные.

### Irrefutable

```rust
let x = 5;
```

Здесь `x` — это паттерн. Он совпадает с *любым* значением.
`let` принимает только неопровержимые паттерны. Потому что если паттерн не совпадет, что делать переменной? Быть в суперпозиции?

### Refutable

```rust
if let Some(x) = a_value { ... }
```

Здесь `Some(x)` — это паттерн. Если `a_value` равно `None`, паттерн *не совпадет*.
И это нормально для `if let`, `while let` и `match`. Они ожидают, что может быть облом.

### Почему это важно?

Если вы попробуете сделать так:

```rust
let Some(x) = some_option_value;
```

Компилятор даст вам по рукам. Потому что "А что, если там None?!".
Вы должны обработать все варианты.

Ну, или использовать `let...else`, если вы смелый:

```rust
let Some(x) = some_option_value else {
    panic!("Все пропало!");
};
```

### Итог

- `let`, `for`, параметры функций — только **Irrefutable**.
- `if let`, `while let`, `match` — могут быть **Refutable**.

---

**Анекдот:**

> — Доктор, меня все игнорируют.  
> — Следующий!
> (Паттерн пациента оказался Refutable и не совпал с ожиданиями врача).

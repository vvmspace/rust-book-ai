## Потоки: Запускаем код одновременно 🧵

Многозадачность в Rust — это просто. И страшно.

### `thread::spawn`

```rust
use std::thread;
use std::time::Duration;

let handle = thread::spawn(|| {
    for i in 1..10 {
        println!("Привет из потока №{}", i);
        thread::sleep(Duration::from_millis(1));
    }
});

handle.join().unwrap(); // Ждем, пока поток закончит работу
```

### Ключевое слово `move`

Если вы хотите использовать данные из главного потока внутри нового потока, вам нужно **переместить** (move) их туда.

```rust
let v = vec![1, 2, 3];

let handle = thread::spawn(move || {
    println!("Вот вектор: {:?}", v);
});
```

Без `move` Rust будет ругаться, что переменная может прожить дольше, чем ссылка на неё. С `move` поток забирает `v` с собой в моги... кхм, в свой стек.

---

**Анекдот:**

> — Тук-тук.  
> — Кто там?  
> — (опережая события) Гонка данных!

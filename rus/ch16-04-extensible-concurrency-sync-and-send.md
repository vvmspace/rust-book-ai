## `Send` и `Sync`: Пропуск в мир многопоточности 🎫

В Rust конкурентность встроена не в библиотеку, а в саму систему типов.
Есть два магических трейта (Marker Traits):

### `Send`: "Меня можно отправить"
Трейт `Send` означает, что владение типом можно передать в другой поток.

- Почти всё в Rust — `Send`.
- Исключение: `Rc<T>` (он не атомарный, его нельзя двигать между потоками), сырые указатели (черная магия).

### `Sync`: "Меня можно расшарить"
Трейт `Sync` означает, что на тип можно безопасно ссылаться (`&T`) из нескольких потоков одновременно.

- Тип `T` является `Sync`, если `&T` является `Send`.
- `Mutex<T>` — это `Sync`.
- `RefCell<T>` — это **не** `Sync` (он проверяет заимствования в рантайме, но не атомарно).

### Реализация вручную
Вы можете реализовать эти трейты вручную (`unsafe impl Send for MyType`), но это как отключить систему стабилизации в спорткаре. Делайте это только если вы точно знаете, что делаете, и прочитали "Rustonomicon" три раза перед сном.

---

**Анекдот:**

> — Алло, это техподдержка? Я пытаюсь отправить `Rc<i32>` в другой поток, а компилятор ругается.  
> — А вы пробовали обернуть его в `Arc`?  
> — Нет, я просто написал `unsafe impl Send`.  
> — (Звуки паники на заднем плане) Сэр, немедленно отойдите от клавиатуры, к вам выехал наряд `rustc`!

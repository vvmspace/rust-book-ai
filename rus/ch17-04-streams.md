## Стримы: Фьючеры в очереди 🌊

Если `Future` — это обещание одного значения, то `Stream` — это обещание серии значений.
Это как `Iterator`, только `Async`.

```rust
// Iterator
let item = iter.next(); // Блокирует поток

// Stream
let item = stream.next().await; // Не блокирует, ждет
```

### `Stream` vs `StreamExt`

В стандартной библиотеке (пока) нет удобных методов для стримов. Они живут в трейте `StreamExt` (обычно из крейта `futures` или `tokio`).
Поэтому, если компилятор орет "no method named `next`", проверьте:

1. Подключили ли вы `use futures::stream::StreamExt;`?
2. Точно ли это стрим, а не итератор?

### Как создать стрим?

Самый простой способ — превратить итератор в стрим:

```rust
let stream = tokio_stream::iter(vec![1, 2, 3]);

while let Some(value) = stream.next().await {
    println!("Got: {}", value);
}
```

Это очень удобно для обработки данных, которые приходят кусками (сеть, файлы, каналы).

---

**Анекдот:**

> Два стрима заходят в бар.
> Один говорит: "Мне пива".
> Бармен: "Подождите, сейчас `await` закончится".
> Второй говорит: "А мне тоже пива, но я через `buffer_unordered(10)`".
> Бармен падает в обморок от переполнения буфера.

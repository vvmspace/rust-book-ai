<!-- Old headings. Do not remove or links may break. -->

<a id="turning-our-single-threaded-server-into-a-multithreaded-server"></a>
<a id="from-single-threaded-to-multithreaded-server"></a>

## Многопоточный сервер: давим на газ 🏎️

Сейчас наш сервер работает как улитка в очереди за кофе: обслуживает одного клиента, пока другие ждут. Если кто-то закажет капучино с соевым молоком и сиропом (тяжелый запрос), вся очередь встанет. Это неприемлемо! Мы хотим скорость. Мы хотим мощь.

### Имитируем тормоза 🐌

Давайте специально заставим сервер тупить, чтобы понять проблему. Добавим эндпоинт `/sleep`, который будет спать 5 секунд перед ответом.

<Listing number="21-10" file-name="src/main.rs" caption="Имитация медленного запроса с помощью sleep">

```rust,no_run
{{#rustdoc_include ../listings/ch21-web-server/listing-21-10/src/main.rs:here}}
```

</Listing>

Запустите сервер (`cargo run`). Откройте две вкладки: одну с `127.0.0.1:7878/sleep`, другую просто `127.0.0.1:7878`.
Запустите `/sleep`. Быстро переключитесь и запустите вторую.
Видите? Вторая вкладка крутится и ждет, пока первая не выспится.

Вот оно — бутылочное горлышко однопоточности.

### Решение: Пул Потоков (Thread Pool) 🏊‍♂️

Мы могли бы создавать новый поток на каждый запрос (`thread::spawn`). Но представьте, что придет 10 миллионов пользователей. Ваш сервер просто взорвется от такого количества потоков (DoS своими руками).

Нам нужен **Пул Потоков**. Это фиксированная группа "работяг" (threads), которые ждут задачи. Пришел запрос -> свободный работяга взял, обработал, вернулся в строй. Если все заняты — запрос ждет в очереди.

Это баланс между скоростью и защитой ресурсов.

### Разработка через компилятор (Compiler-Driven Development) 🛠️

Мы не будем писать реализацию сразу. Сначала мы напишем код, который *хотим* видеть в `main.rs`, а потом заставим компилятор сказать нам, чего не хватает. Это как TDD, только круче.

Вот наш идеальный интерфейс:

<Listing number="21-12" file-name="src/main.rs" caption="Наш идеальный интерфейс ThreadPool">

```rust,ignore,does_not_compile
{{#rustdoc_include ../listings/ch21-web-server/listing-21-12/src/main.rs:here}}
```

</Listing>

Компилятор сразу начнет ругаться: "Что такое ThreadPool?".

```console
{{#include ../listings/ch21-web-server/listing-21-12/output.txt}}
```

Окей, окей. Создаем `src/lib.rs` и структуру `ThreadPool`:

<Listing file-name="src/lib.rs">

```rust,noplayground
{{#rustdoc_include ../listings/ch21-web-server/no-listing-01-define-threadpool-struct/src/lib.rs}}
```

</Listing>

Теперь он жалуется на отсутствие метода `new`. Справедливо. Реализуем:

<Listing file-name="src/lib.rs">

```rust,noplayground
{{#rustdoc_include ../listings/ch21-web-server/no-listing-02-impl-threadpool-new/src/lib.rs}}
```

</Listing>

Дальше: нет метода `execute`. И тут начинается самое интересное. Какой тип замыкания нам нужен?
Вспомним `thread::spawn`. Ему нужно `FnOnce`, `Send` и `'static`.

1. `FnOnce`: запрос выполняется один раз.
2. `Send`: мы передаем замыкание из одного потока в другой.
3. `'static`: мы не знаем, сколько поток будет работать.

Пишем сигнатуру:

<Listing file-name="src/lib.rs">

```rust,noplayground
{{#rustdoc_include ../listings/ch21-web-server/no-listing-03-define-execute/src/lib.rs:here}}
```

</Listing>

Оно компилируется! Но... ничего не делает. Пул пуст.

#### Валидация: защита от дурака

Если кто-то попытается создать пул с 0 потоков, программа должна паниковать. Ноль рабочих ничего не сделают.

<Listing number="21-13" file-name="src/lib.rs" caption="Паникуем, если size == 0">

```rust,noplayground
{{#rustdoc_include ../listings/ch21-web-server/listing-21-13/src/lib.rs:here}}
```

</Listing>

### Хранилище для рабочих 👷

Нам нужно где-то хранить наши потоки. Используем `Vec`. Но что именно хранить?
`thread::spawn` возвращает `JoinHandle`.

<Listing number="21-14" file-name="src/lib.rs" caption="Создаем вектор для хранения потоков">

```rust,ignore,not_desired_behavior
{{#rustdoc_include ../listings/ch21-web-server/listing-21-14/src/lib.rs:here}}
```

</Listing>

Всё круто, но есть проблема. `thread::spawn` запускает поток *сразу* и дает ему задачу. А нам нужно создать поток, который будет *ждать* задачу.
Стандартная библиотека так не умеет. Придется городить своего **Worker**-а.

#### Worker: посредник между боссом и работой

`Worker` — это структура, которая будет хранить поток и `id`.

1. `ThreadPool` создает канал (channel).
2. `ThreadPool` держит отправляющий конец (Sender).
3. Каждый `Worker` держит принимающий конец (Receiver).

Но стоп! Канал в Rust — это **MPSC** (Multiple Producer, Single Consumer). Много отправителей, ОДИН получатель. А у нас много воркеров, которые хотят слушать одну очередь.

Нам нужно расшарить `Receiver` между потоками.
Как расшарить владение? `Arc`.
Как разрешить изменять (читать из канала) одновременно? `Mutex`.

**Super-Combo:** `Arc<Mutex<Receiver<Job>>>`.

<Listing number="21-18" file-name="src/lib.rs" caption="Шарим receiver через Arc и Mutex">

```rust,noplayground
{{#rustdoc_include ../listings/ch21-web-server/listing-21-18/src/lib.rs:here}}
```

</Listing>

Это выглядит страшно, но это работает! Мы клонируем `Arc` для каждого воркера, и они все могут безопасно бороться за блокировку мьютекса, чтобы получить задачу.

### Отправка задач 📨

В методе `execute` мы теперь просто отправляем задачу в канал.

<Listing number="21-19" file-name="src/lib.rs" caption="Отправляем Job в канал">

```rust,noplayground
{{#rustdoc_include ../listings/ch21-web-server/listing-21-19/src/lib.rs:here}}
```

</Listing>

А воркер в своем потоке бесконечно крутит цикл: "Взять блок -> Получить задачу -> Выполнить".

<Listing number="21-20" file-name="src/lib.rs" caption="Воркер исполняет задачи">

```rust,noplayground
{{#rustdoc_include ../listings/ch21-web-server/listing-21-20/src/lib.rs:here}}
```

</Listing>

Кстати, тут есть тонкий момент с `while let` против `let job = ...`. Если использовать `while let` с мьютексом, блокировка будет висеть до конца блока. Это убьет параллелизм! Поэтому мы явно получаем задачу, `job` выпадает из области блокировки, и только потом выполняется. Другие воркеры могут брать следующие задачи в это время.

### Итог

Запускайте! `cargo run`.
Теперь ваши запросы к `/sleep` не блокируют остальные. Сервер жужжит всем пулом. Кайф? Кайф.

---

**Анекдот в тему:**

> — Тук-тук.  
> — Кто там?  
> — Гонка состояний.  
> — Гонка сост...  
> — (Врывается) Проходите!
